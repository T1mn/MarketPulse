/**
 * @marketpulse/web
 * React Web Client
 *
 * NOTE: This package will replace the current frontend/ directory.
 * The existing React code can be migrated here.
 *
 * Migration steps:
 * 1. Copy frontend/src/* to packages/web/src/
 * 2. Update imports to use @marketpulse/sdk
 * 3. Update vite.config.ts
 * 4. Remove old frontend/ directory
 */

export const WEB_PLACEHOLDER = true
